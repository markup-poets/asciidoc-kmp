:toc: left
:toclevels: 2
:numbered: 

= My First Article: A Guide to Kotlin Multiplatform

John Doe <john@example.com> v1.0, 2026-01-23

== Introduction

Welcome to my comprehensive article about Kotlin Multiplatform development. This article demonstrates how to organize content across multiple files for better maintainability.

In this article, we'll explore:

* Getting started with KMP
* Best practices for project structure
* Advanced techniques

Let's dive in!

== Chapter 1: Getting Started with Kotlin Multiplatform

Kotlin Multiplatform (KMP) allows you to share code between different platforms while maintaining the flexibility to write platform-specific code when needed.

=== Why Choose KMP?

There are several compelling reasons to use Kotlin Multiplatform:

* Code Reusability: Write business logic once, use everywhere
* Type Safety: Leverage Kotlin's strong type system
* Native Performance: Compile to native code for each platform
* Gradual Adoption: Start small and expand shared code over time

=== Setting Up Your First Project

To get started with KMP, you'll need:

* Kotlin 2.0 or newer
* Gradle build system
* Platform-specific SDKs (Android SDK, Xcode for iOS)

Here's an example of common code:

----
// Example: Common code
expect fun getPlatformName(): String

fun greet(): String {
    return "Hello from ${getPlatformName()}!"
}
----

=== Project Structure

A typical KMP project follows this structure:

----
project/
├── commonMain/     # Shared code
├── androidMain/    # Android-specific
├── iosMain/        # iOS-specific
└── jvmMain/        # JVM-specific
----

This modular structure keeps platform-specific code isolated while maximizing code sharing.

=== Your First Shared Function

Let's create a simple function that works across all platforms:

----
// In commonMain
fun calculateFibonacci(n: Int): List<Int> {
    if (n <= 0) return emptyList()
    if (n == 1) return listOf(0)
    
    val sequence = mutableListOf(0, 1)
    for (i in 2 until n) {
        sequence.add(sequence[i-1] + sequence[i-2])
    }
    return sequence
}
----

This function works identically on Android, iOS, JVM, and native platforms!

Start with pure business logic in commonMain before adding platform-specific features.

== Chapter 2: Advanced Techniques

Once you're comfortable with the basics, you can explore more advanced KMP patterns and techniques.

=== Expect/Actual Declarations

The expect/actual mechanism is the foundation of platform-specific code in KMP.

In your commonMain source set:

----
// Declare what you expect each platform to provide
expect class PlatformLogger {
    fun log(message: String)
}

expect fun currentTimeMillis(): Long
----

In androidMain:

----
actual class PlatformLogger {
    actual fun log(message: String) {
        android.util.Log.d("MyApp", message)
    }
}

actual fun currentTimeMillis(): Long {
    return System.currentTimeMillis()
}
----

=== Dependency Injection

For larger projects, consider using dependency injection to manage platform-specific implementations:

----
// Common interface
interface FileSystem {
    fun readFile(path: String): String
    fun writeFile(path: String, content: String)
}

// Inject platform-specific implementation
class DocumentProcessor(
    private val fileSystem: FileSystem
) {
    fun processDocument(path: String): String {
        val content = fileSystem.readFile(path)
        return content.uppercase()
    }
}
----

Keep platform-specific code minimal. Most of your logic should be in commonMain.

=== Testing Across Platforms

Write tests in commonTest that run on all platforms:

----
class CalculatorTest {
    @Test
    fun testAddition() {
        val result = Calculator.add(2, 3)
        assertEquals(5, result)
    }
    
    @Test
    fun testFibonacci() {
        val sequence = calculateFibonacci(5)
        assertEquals(listOf(0, 1, 1, 2, 3), sequence)
    }
}
----

These tests automatically run on JVM, Android, iOS, and other configured platforms.

=== Best Practices Summary

* Maximize shared code: Put as much logic as possible in commonMain
* Minimize expect/actual: Use only when truly platform-specific
* Test thoroughly: Run tests on all target platforms
* Document platform differences: Clearly explain why platform-specific code exists
* Use interfaces: Prefer interfaces over expect/actual when possible

== Conclusion

We've covered the fundamentals of Kotlin Multiplatform development. The modular approach to content organization makes it easy to maintain and update individual sections.

=== Key Takeaways

* KMP enables true code sharing across platforms
* Proper project structure is essential
* Testing across platforms ensures quality

Thank you for reading!

== About the Author

John Doe is a software developer passionate about multiplatform development and technical writing.

