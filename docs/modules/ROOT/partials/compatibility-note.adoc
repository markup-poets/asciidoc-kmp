[NOTE]
====
This project *aims for compatibility* with the official {url-spec}[AsciiDoc Language Specification], tracked via its {url-tck}[Technology Compatibility Kit (TCK)]: {tck-passing} of {tck-total} published TCK tests pass against commit `{tck-commit-short}` using the official Node.js harness. This is a snapshot, not a certification or a completeness claim — the TCK itself is still growing alongside the specification.
====
